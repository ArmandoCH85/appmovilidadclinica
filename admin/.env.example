# Copiar a `admin/.env` (Vite lo lee en build/dev). NO versionar `admin/.env`.
#
# Base del API. Con `/api` funciona detras de un reverse proxy (nginx) que
# enruta /api/* al backend. Para desarrollo contra un backend local se puede
# usar http://127.0.0.1:8080/api
VITE_API_BASE_URL=/api
