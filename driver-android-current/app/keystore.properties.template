# ============================================
# Plantilla de configuración de firma release.
# Copiar como: driver-android/app/keystore.properties
# Llenar con los valores reales.
# NO COMMITEAR (ya está en .gitignore).
# ============================================

# Ruta al archivo de la keystore (relativa al módulo app/, NO incluir prefijo app/)
# Recomendado: upload-keystore.jks (junto a este .properties)
storeFile=upload-keystore.jks

# Alias de la clave dentro de la keystore
keyAlias=upload

# Password del keystore y de la clave (si son iguales, repetir)
storePassword=TU_PASSWORD_AQUI
keyPassword=TU_PASSWORD_AQUI
